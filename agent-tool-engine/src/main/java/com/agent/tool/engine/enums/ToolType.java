package com.agent.tool.engine.enums;

/**
 * Tool type (doc 01-database §4.1 tool_registry.tool_type).
 *
 * <p>ATOMIC=原子工具, COMPOSITE=复合工具, AGENT=Agent 型工具.</p>
 */
public enum ToolType {

    ATOMIC,
    COMPOSITE,
    AGENT
}
